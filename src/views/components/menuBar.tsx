import { useState } from "react";
import type { ActiveUserProfile } from "../../data/models/activeUserProfile";

export default function MenuBar() {
  const [userProfileCard, setUserProfileCard] = useState<ActiveUserProfile>();

  return <div>

  </div>
}